import React from "react"
import styles from "./custom-inpt.module.scss"

export const Input = () => {
    return (
        <div className={styles.container}>
            <input type="text"/>
        </div>

    )
}